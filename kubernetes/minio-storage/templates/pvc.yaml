apiVersion: v1
kind: PersistentVolumeClaim
metadata:
    name: minio-local-pvc
spec:
    accessModes:
        - ReadWriteOnce
    resources:
        requests:
            storage: 5Gi
    storageClassName: local-openebs-hostpath-minio
