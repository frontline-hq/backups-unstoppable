---
- name: Create application directory
  file:
    path: "{{ base_app_path }}/{{ client_name }}/{{ project_name }}"
    state: directory
    owner: "{{ app_user }}"
    group: "{{ app_group }}"
    mode: "0750"

- name: Create log directory
  file:
    path: "{{ base_log_path }}/{{ client_name }}/{{ project_name }}"
    state: directory
    owner: "{{ app_user }}"
    group: "{{ app_group }}"
    mode: "0750"

- name: Get UID of app user
  command: id -u {{ app_user }}
  register: app_user_uid_output
  changed_when: false

- name: Get GID of app group
  command: id -g {{ app_user }}
  register: app_group_gid_output
  changed_when: false

- name: Set UID/GID facts
  set_fact:
    app_user_uid: "{{ app_user_uid_output.stdout }}"
    app_group_gid: "{{ app_group_gid_output.stdout }}"

- name: Copy script files
  copy:
    src: "{{ playbook_dir }}/../src/"
    dest: "{{ base_app_path }}/{{ client_name }}/{{ project_name }}/src/"
    owner: "{{ app_user }}"
    group: "{{ app_group }}"
    mode: preserve

- name: Generate backup data for SFTP
  block:
    - name: Create SFTP data directory
      file:
        path: "{{ base_app_path }}/{{ client_name }}/{{ project_name }}/persisted/sftp"
        state: directory
        owner: "{{ app_user }}"
        group: "{{ app_group }}"
        mode: "0750"

    - name: Generate backup data
      command:
        cmd: >-
          {{ base_app_path }}/{{ client_name }}/{{ project_name }}/src/genbackupdata.sh
          -c {{ backup_size }}
          -s {{ backup_seed }}
          -f 16384
          -d 3
          -m 4
          {{ base_app_path }}/{{ client_name }}/{{ project_name }}/persisted/sftp/backup_data
      args:
        creates: "{{ base_app_path }}/{{ client_name }}/{{ project_name }}/persisted/sftp/backup_data"
      register: backup_result
      async: 600 # 10 minutes timeout
      poll: 10 # Check every 10 seconds
      when: project_vars.create_sftp | bool
      environment:
        PATH: "/usr/local/bin:/usr/bin:/bin" # Ensure openssl is available

    - name: Debug backup generation
      debug:
        msg:
          - "Backup generation status: {{ backup_result.stdout if backup_result.stdout is defined else 'No output' }}"
          - "Errors (if any): {{ backup_result.stderr if backup_result.stderr is defined else 'No errors' }}"
      when: backup_result is changed
  when: project_vars.create_sftp | bool

- name: Copy project files
  copy:
    src: "{{ playbook_dir }}/../vars/production/{{ client_name }}/{{ project_name }}/"
    dest: "{{ base_app_path }}/{{ client_name }}/{{ project_name }}/vars/"
    owner: "{{ app_user }}"
    group: "{{ app_group }}"
    mode: "0640"
  become: true
  become_user: "{{ app_user }}"

- name: Deploy run script
  template:
    src: "{{ playbook_dir }}/templates/run.sh.j2"
    dest: "{{ base_app_path }}/{{ client_name }}/{{ project_name }}/run.sh"
    owner: "{{ app_user }}"
    group: "{{ app_group }}"
    mode: "0750"
  become: true
  become_user: "{{ app_user }}"

- name: Deploy docker-compose file
  template:
    src: "{{ playbook_dir }}/templates/docker-compose.yml.j2"
    dest: "{{ base_app_path }}/{{ client_name }}/{{ project_name }}/docker-compose.yml"
    owner: "{{ app_user }}"
    group: "{{ app_group }}"
    mode: "0640"
  become: true
  become_user: "{{ app_user }}"

- name: Test backup system
  block:
    - name: Run initial backup test
      command:
        cmd: "./run.sh"
        chdir: "{{ base_app_path }}/{{ client_name }}/{{ project_name }}"
      become: true
      become_user: "{{ app_user }}"
      register: backup_test
      changed_when: true
      environment:
        HOME: "/home/{{ app_user }}"
      async: 1800
      poll: 10

    - name: Display backup test output
      debug:
        msg: "{{ backup_test.stdout_lines }}"
      when: backup_test.stdout_lines is defined

    - name: Display backup test errors
      debug:
        msg: "{{ backup_test.stderr_lines }}"
      when: backup_test.stderr_lines is defined

    - name: Install cron job with flock
      cron:
        name: "Run {{ client_name }} - {{ project_name }}"
        user: "{{ app_user }}"
        job: "cd {{ base_app_path }}/{{ client_name }}/{{ project_name }} && flock -n {{ lock_file_path }}/{{ client_name }}_{{ project_name }}.lock ./run.sh >> {{ base_log_path }}/{{ client_name }}/{{ project_name }}/app.log 2>&1"
        minute: "{{ project_vars.schedule.split(' ')[0] }}"
        hour: "{{ project_vars.schedule.split(' ')[1] }}"
        day: "{{ project_vars.schedule.split(' ')[2] }}"
        month: "{{ project_vars.schedule.split(' ')[3] }}"
        weekday: "{{ project_vars.schedule.split(' ')[4] }}"
      become: true
      when: not ansible_check_mode

  rescue:
    - name: Backup test failed
      fail:
        msg: |
          Initial backup test failed.
          stdout: {{ backup_test.stdout if backup_test.stdout is defined else 'N/A' }}
          stderr: {{ backup_test.stderr if backup_test.stderr is defined else 'N/A' }}
          rc: {{ backup_test.rc if backup_test.rc is defined else 'N/A' }}
